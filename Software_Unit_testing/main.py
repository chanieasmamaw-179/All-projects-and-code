def my_sum(nums):
    """
    Checks if the given list of numbers is empty or not.
    Returns False if the list is empty, True otherwise.
    """
    if len(nums) == 0:  # Check if the list is empty
        return False
    return True  # If the list is not empty, return True
